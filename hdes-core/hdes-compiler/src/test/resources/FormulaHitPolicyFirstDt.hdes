decisiontable FormulaHitPolicyFirstDt (
    a,b, c: DECIMAL,
    total: DECIMAL = a + b + c
  ) : { value: INTEGER, qualifier: DECIMAL = value + 10 + total } 

{
  
  findFirst(){
    when( undefined, undefined, undefined, undefined ) add(20)
  }
}