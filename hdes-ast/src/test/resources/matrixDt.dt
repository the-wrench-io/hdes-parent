id: MatrixDT
headers: {
  IN STRING name,
  IN STRING lastName,
  
  OUT INTEGER value,
  OUT FORMULA INTEGER total: 'sum(outputs)/size(outputs) + 10/50' 
} MATRIX: {
            { 'BOB', 'SAM', ? }
  name:     {  10,    20,   30 },
  lastName: {  20,    50,   60 }
}