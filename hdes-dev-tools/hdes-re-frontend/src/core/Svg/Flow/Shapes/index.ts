import {default as Start} from './StartShape';
import {default as End} from './EndShape';
import {default as Decision} from './DecisionShape'
import {default as Task} from './TaskShape'

const Shapes = {
  Start, End, Decision, Task
}

export default Shapes;